#ifndef _KISMET_REQUISITES_H_
#define _KISMET_REQUISITES_H_

#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <map>
#include <Simple2D.h>

#include "core/kismet_types.h"
#include "core/kismet_id_allocator.h"

#define max(a,b) (((a) > (b)) ? (a) : (b))
#define min(a,b) (((a) < (b)) ? (a) : (b))

//#define k_invalid_int 0xFFFF
//#define k_invalid_float 0xFFFFFFFF

#endif // !_KISMET_REQUISITES_H_
